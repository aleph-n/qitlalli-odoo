# Environment variables for LOCAL DEVELOPMENT ONLY
# Copy this file to .env and adjust values as needed
# 
# 🔐 SECURITY NOTE: Production uses Google Secret Manager
# No sensitive data should be committed to the repository

# Database configuration (local development)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=qitlalli_db
DB_USER=odoo
DB_PASSWORD=myodoo

# Odoo configuration (local development)
ODOO_RC=/etc/odoo/odoo.conf
ODOO_ADMIN_PASSWD=admin

# Development settings
ODOO_DEV_MODE=reload,qweb,werkzeug,xml

# Email configuration (local testing - use app passwords)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-gmail-app-password

# JWT secret (local development)
JWT_SECRET=local-jwt-secret-key

# WhatsApp API configuration (for future integration)
WHATSAPP_API_URL=https://api.whatsapp.com/send
WHATSAPP_API_TOKEN=your-test-token

# GCP Configuration (for deployment commands)
GCP_PROJECT_ID=qitlalli-project
GCP_REGION=us-central1
GOOGLE_CLOUD_SQL_CONNECTION_NAME=qitlalli-project:us-central1:qitlalli-db

# Local development flags
PRODUCTION=false
DEBUG=true

# 🚨 PRODUCTION SECURITY:
# - All sensitive values are stored in Google Secret Manager
# - Service accounts use IAM-based authentication
# - No passwords or tokens in environment variables
# - Secrets are automatically injected by Cloud Run